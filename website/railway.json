{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "npx sirv-cli dist --host 0.0.0.0 --port $PORT --single --gzip --brotli",
        "restartPolicyType": "ON_FAILURE"
    }
}